# Roadmap VibeRDP

Один чекбокс — одна итерация: сделал, проверил, отметил `[x]`, коротко отчитался, остановился.
Выполненный пункт — `[x]` с датой, веткой и сутью сделанного.
Задачи — дословно из раздела 5 [idea.md](idea.md); «см.» ведёт на уточнение из [базы знаний](knowledge/README.md), найденное при сверке спецификации с документацией.
Решения на развилках — [decisions.md](decisions.md).

## Этап 0 — Каркас
- [x] **0.1 Монорепо по структуре выше, `.gitignore`, лицензия Apache-2.0, README-заглушка** — ✅ (2026-09-24, `next`) дерево по §2 [idea.md](idea.md), документы — по стандарту семейства ([карта путей](README.md)); `.vibe/` из эталона VibeBrains `c1f9476` (95 файлов, sha256 сверены), правила проекта — `.vibe/rules/viberdp.mdc` и `CLAUDE.md`; лицензия — эталон apache.org; ветки `main` и `next`; пять уточнений к спецификации — в [базе знаний](knowledge/README.md)
- [x] **0.2 FreeRDP 3.x как submodule в `core/third_party`, скрипт сборки universal static libs (arm64+x86_64) с нужными каналами (cliprdr, rail, disp, rdpgfx, drdynvc)** — ✅ (2026-09-24, `next`) FreeRDP 3.32.0 — подписанный тег, неглубокий подмодуль; OpenSSL 3.5.8 LTS — подпись проверена, sha256 в `build.env`; `core/scripts/build-freerdp.sh` собирает каждую архитектуру отдельно и склеивает `lipo`; зашитый префикс `/opt/viberdp`, OpenSSL без подгрузки модулей; скрипт проверяет срезы, macOS 14.0, пути в бинарях и линковку через пакеты CMake на arm64 и x86_64 — [devSetup.md](manuals/devSetup.md), [buildMacOS.md](knowledge/freerdp/buildMacOS.md)
- [x] **Исправление 0.2: FreeRDP падал на macOS младше 27** — ✅ (2026-09-24, `next`) проверка FreeRDP приняла `pipe2` из SDK Xcode 27, и каждое создание контекста падало на macOS 26; `WINPR_HAVE_PIPE2=OFF`, API новее минимальной macOS — ошибка компиляции, слабые ссылки ловит проверка скрипта — [buildMacOS.md](knowledge/freerdp/buildMacOS.md); срез x86_64 проверки линковки прогнан после исправления
- [x] **0.3 `VibeRDPCore`: плоский C API (create/connect/disconnect/callbacks), CMake-таргет** — ✅ (2026-09-24, `next`) `core/include/VibeRDPCore/VibeRDPCore.h` и `core/src/session.c`; `core/scripts/build-core.sh` собирает arm64, x86_64 и сборку с санитайзерами и склеивает универсальную `libVibeRDPCore.a` (с 0.4 — `VibeRDPCore.framework`); тесты на фейковых TCP-серверах и импорт в Swift: arm64 7/7, санитайзеры 6/6, x86_64 под Rosetta 7/7 — см. [toolingHangs.md](knowledge/macos/toolingHangs.md)
- [x] **0.4 `client-macos` через XcodeGen, пустое окно приложения, линковка с `VibeRDPCore`** — ✅ (2026-09-24, `next`) ядро стало динамическим `VibeRDPCore.framework` с экспортом только API `VRC*` — [решение 11](decisions.md); `client-macos/project.yml` для XcodeGen 2.46.0 (закреплён в `build.env`), AppKit без storyboard, главное окно и меню, строки меню — из базового каталога на русском; `client-macos/scripts/build-client.sh` генерирует проект, собирает универсальное приложение, проверяет срезы, macOS 14, встроенный фреймворк и подпись; тесты внутри приложения: arm64 8/8, x86_64 8/8 — [devSetup.md](manuals/devSetup.md), [xcodeClient.md](knowledge/macos/xcodeClient.md)
- [x] **0.5 `helper-win` — cargo-проект, пустой exe, сборка в CI** — ✅ (2026-09-24, `next`) exe без консоли, C-рантайм внутри (`+crt-static`), тулчейн 1.97.1 закреплён; задача CI `helper` на `windows-2025` собирает exe, `helper-win/scripts/check-exe.ps1` подтверждает x64, GUI-подсистему и зависимости только от системных `KERNEL32.dll`, `ntdll.dll` и `api-ms-win-core-synch`; exe — артефакт `vibe-seam-helper` — [helperBuild.md](knowledge/windows/helperBuild.md)
- [x] **0.6 GitHub Actions: сборка клиента (macOS) и хелпера (Windows) на каждый push** — ✅ (2026-09-24, `next`) `.github/workflows/ci.yml` в публичном `borodatych/VibeRDP`: `macos-26` с Xcode 26.6 собирает зависимости, ядро и клиент, тесты идут и под x86_64 через Rosetta раннера; Windows — exe хелпера; линт — shellcheck и actionlint закреплённых версий; приложение — артефакт `VibeRDP-app` — [решение 13](decisions.md), [githubActions.md](knowledge/ci/githubActions.md)

## Этап 1 — Базовый RDP (Desktop-режим)
- [x] **1.1 Подключение с NLA, диалог сертификата с запоминанием отпечатка** — ✅ (2026-09-24, `next`) ядро отдаёт цепочку приложению (`ExternalCertificateManagement`) и ждёт ответа вместе с отменой; RDP Security выключен, консольные колбэки FreeRDP обнулены, ошибки сведены к категориям `VRCErrorKind`; клиент: форма подключения, SecTrust, диалог с отпечатком SHA-256, запомненные отпечатки в `UserDefaults`, предупреждение о сменившемся сертификате — [решение 14](decisions.md), [clientLifecycle.md](knowledge/freerdp/clientLifecycle.md), [certificateTrust.md](knowledge/macos/certificateTrust.md); тесты: ядро на фейковом TLS-сервере — arm64 14/14, санитайзеры 13/13, x86_64 14/14; клиент — 27/27 на arm64 и x86_64, CI зелёный на Xcode 26.6; приложение объявляет доступ к локальной сети (`NSLocalNetworkUsageDescription`) — [localNetworkPrivacy.md](knowledge/macos/localNetworkPrivacy.md); **живое подключение требует проверки оператором** — [liveChecks.md](manuals/liveChecks.md), раздел 1
- [x] **1.2 Рендер фреймбуфера в Metal (RDPGFX, H.264 при наличии), окно и полноэкранный режим** — ✅ (2026-09-25, `next`) ядро рисует прямо в IOSurface формата BGRA (`gdi_init_ex` с `PIXEL_FORMAT_BGRX32`), колбэки `frameResized` и `frameUpdated`, `VRCSessionCopyFrameSurface`; RDPGFX и RemoteFX включены, смена размера сервером даёт новую поверхность; клиент: `DesktopView` на `CAMetalLayer`, `FrameRenderer` — копия или масштаб `MPSImageBilinearScale` с полями, без своих шейдеров и без Metal Toolchain; рабочий стол закрывает форму вместе с кнопкой, и сессию завершает пункт «Отключиться» в меню «Файл»; имя хоста — в подзаголовке окна; пункт «Во весь экран» с системным fn-F — [решение 15](decisions.md), [graphicsOutput.md](knowledge/freerdp/graphicsOutput.md), [metalRendering.md](knowledge/macos/metalRendering.md); H.264 — задача 1.11; тесты: ядро — arm64 16/16, санитайзеры 15/15, x86_64 16/16; клиент — 39/39 на arm64 и x86_64, в том числе живой тест с sample-сервером FreeRDP, который проигрывает запись RemoteFX; CI зелёный, в нём ни один тест Metal не пропущен — [sampleServer.md](knowledge/freerdp/sampleServer.md); сервер запускает скрипт сборки — иначе macOS держит запуск вопросом о съёмном томе, [removableVolumePrivacy.md](knowledge/macos/removableVolumePrivacy.md); у каждого теста предел в минуту; **картинку с настоящего Windows проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 2
- [x] **1.3 Мышь и колесо, включая трекпад-скролл с инерцией** — ✅ (2026-09-25, `next`) очередь ввода в ядре: главный поток не ждёт сети, отправляет поток сессии, до ACTIVE события ждут в своём порядке, перемещения сливаются; кнопки, в том числе «назад» и «вперёд», и колесо в единицах Windows, поделённое на 9-битные шаги; горизонтальное колесо и боковые кнопки — по возможностям сервера; курсор сервера — `NSCursor` в масштабе картинки, просьба сервера передвинуть курсор отбрасывается; клиент: `DesktopView` принимает мышь и первый щелчок, точка вида переводится в пиксель обратным `FrameGeometry`, `WheelAccumulator` — строки колеса и точки трекпада с накоплением дробей, инерция идёт как есть — [решение 16](decisions.md), [pointerInput.md](knowledge/freerdp/pointerInput.md), [mouseInput.md](knowledge/macos/mouseInput.md); тесты: ядро — arm64 27/27, санитайзеры 26/26, x86_64 27/27; клиент — 57/57 на arm64 и x86_64, в том числе живой тест ввода на интерактивном sample-сервере — [sampleServer.md](knowledge/freerdp/sampleServer.md); **мышь, прокрутку и курсор на настоящем Windows проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 3
- [x] **1.4 Клавиатура: скан-коды, маппинг Cmd/Option, настраиваемые сочетания — и сочетание для «Отключиться»: с 1.2 у пункта его нет, чтобы ⌘-комбинации доставались удалённому рабочему столу** — ✅ (2026-09-25, `next`) ядро: клавиши, фокус и отпускание нажатых идут той же очередью, что мышь, — `VRCSessionSendKey`, `VRCSessionSendFocusIn` с Caps Lock и всегда включённым Num Lock, `VRCSessionReleaseKeys`; поток сессии помнит нажатые на сервере клавиши, Pause — последовательностью mstsc; клиент: своя таблица «клавиша Мака → скан-код» вместо таблицы winpr, модификаторы по сторонам — по умолчанию левая ⌘ — Ctrl, правая — Win, набор «Как на PC»; клавиши берёт локальный монитор AppKit до меню, сочетания Мака (⌘Q, ⌘H, ⌥⌘H, ⌘M, ⌘ запятая, fn-F, ⌃⌘F) уходят AppKit целиком; «Отключиться» — ⌥⌘W; окно «Настройки…» с вкладкой «Клавиатура» на SwiftUI, галочка для клавиатур ISO — [решение 17](decisions.md), [freerdp/keyboardInput.md](knowledge/freerdp/keyboardInput.md), [macos/keyboardInput.md](knowledge/macos/keyboardInput.md); гейт слабых ссылок пропускает охраняемые символы Swift — [xcodeClient.md](knowledge/macos/xcodeClient.md); тесты: ядро — arm64 31/31, санитайзеры 30/30, x86_64 31/31; клиент — 89/89 на arm64 и x86_64, в том числе живой тест клавиши на интерактивном sample-сервере; **клавиатуру на настоящем Windows проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 4
- [x] **1.5 Профили подключений, пароли в Keychain, список подключений на SwiftUI — и запрос пароля во время подключения (`AuthenticateEx`) с повтором после неверного** — ✅ (2026-09-25, `next`) ядро спрашивает приложение колбэком `credentialsNeeded`, когда движку не хватает имени или пароля, и ждёт `VRCSessionProvideCredentials` или `VRCSessionCancelCredentials` вместе с прерыванием; отказ завершает сессию без ошибки, копии пароля затираются; клиент: профили в `UserDefaults`, пароли — generic password файловой связки входа, сохраняются только после успешного входа; главное окно — список подключений и редактор на SwiftUI; вопрос об имени и пароле во время подключения и повтор новой сессией после неверного пароля; у профиля свой набор модификаторов — [решение 18](decisions.md), [freerdp/credentials.md](knowledge/freerdp/credentials.md), [macos/keychain.md](knowledge/macos/keychain.md); тесты: ядро — arm64 35/35, санитайзеры 34/34, x86_64 35/35; клиент — 105/105 на arm64 и x86_64, в том числе связка ключей и живой вопрос об учётных данных от sample-сервера; **пароли на настоящем Windows с NLA проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 5
- [x] **1.6 Импорт `.rdp`-файлов** — ✅ (2026-09-25, `next`) свой разбор `RdpFile` по правилам парсера FreeRDP: UTF-16 с меткой и UTF-8, ключи без регистра, `alternate full address` и `server port` сильнее; «Файл → Импортировать файл .rdp…» и двойной щелчок в Finder — импорт и подключение; тип `com.microsoft.uti.rdpfile` импортирован, ранг `Alternate`; повторный импорт выбирает существующее подключение — [решение 19](decisions.md), [rdpFiles.md](knowledge/rdp/rdpFiles.md); тесты: клиент — 113/113 на arm64 и x86_64; **файлы Windows App и mstsc проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 6
- [x] **1.7 RD Gateway** — ✅ (2026-09-25, `next`) ядро: параметры шлюза — адрес, порт, «те же учётные данные», обход для локальной сети, свои имя и пароль; с «теми же» имя и пароль компьютера сразу идут шлюзу; сообщение шлюза — колбэк `gatewayMessage`, обязательное согласие ждёт `VRCSessionResolveGatewayMessage`, без колбэка отклоняется; сертификат и согласие — общий модуль вопросов «да или нет» `decision.c`; клиент: раздел «Шлюз» у подключения, свой пароль шлюза отдельной записью связки ключей, вопрос и повтор пароля шлюза, окно сообщения шлюза, шлюз из `.rdp` — [решение 20](decisions.md), [gateway.md](knowledge/freerdp/gateway.md); тесты: ядро — arm64 40/40, санитайзеры 39/39, x86_64 40/40, в том числе сертификат шлюза первым; клиент — 117/117 на arm64 и x86_64; **настоящий шлюз проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 7
- [x] **1.8 Переподключение при обрыве сети и при сне/пробуждении Мака** — ✅ (2026-09-25, `next`) ядро: `AutoReconnectionEnabled` и цикл `client_auto_reconnect_ex` FreeRDP — только обрыв сети, не конец, выбранный сервером, и не неработающие учётные данные; состояние `Reconnecting`, колбэк `reconnecting` с номером попытки через свой `RetryDialog`, 20 попыток с паузой 5 секунд; отключение пользователя переживает сброс события прерывания; ввод обрыва выбрасывается; `VRCSessionRefresh` — Refresh Rect после пробуждения Мака; клиент: заглушка переподключения с кнопкой «Отключиться», проверка связи по `didWakeNotification`; тестовый sample-сервер рвёт соединение по D — [решение 21](decisions.md), [reconnection.md](knowledge/freerdp/reconnection.md); тесты: ядро — arm64 40/40, санитайзеры 39/39, x86_64 40/40; клиент — 118/118 на arm64 и x86_64, в том числе живое переподключение; **Wi-Fi и сон Мака с настоящим Windows проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 8
- [x] **1.9 Локализация: языки файлами в папке, которую видит пользователь, выбор языка, откат пропавшего языка, гейт каталога — по правилу семейства; база с 0.4 — русский в бинаре (`client-macos/App/Localization.swift`); строки Info.plist (запрос доступа к локальной сети) живут вне каталога — им нужен свой путь через `InfoPlist.strings`** — ✅ (2026-09-25, `next`) папка `~/VibeRDP/lang/`: английский и образец `example.jsonc` засеваются, изменённый файл не перезаписывается, удалённый возвращается; каталог — вшитый русский, поверх правки `ru.json`, поверх выбранный язык, читается один раз при старте; вкладка «Язык»: «Как в системе», список языков, «Открыть папку языков», «Создать файл для правки»; пропавший язык откатывается на системный, затем на русский; `en.lproj/InfoPlist.strings` — вопрос о локальной сети и тип `.rdp`; гейт `check-localization.py` в сборке: русская строка мимо каталога, мёртвый ключ, неполный английский, сломанная подстановка — [решение 22](decisions.md), [localization.md](knowledge/macos/localization.md), спека [langSpec.md](manuals/langSpec.md); тесты: клиент — 125/125 на arm64 и x86_64; **язык системы и перевод вопросов macOS проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 9 — _добавлено вне спецификации, 2026-09-24_
- [x] **1.10 Kerberos в NLA: разбор системного GSS.framework против MIT krb5, выбор, сборка и проверка в домене — сейчас NLA идёт через NTLM, а в доменах политика бывает его запрещает** — ✅ (2026-09-25, `next`) SSP FreeRDP зовёт krb5, а не GSSAPI, прослойку `Kerberos.framework` он отвергает, Heimdal в macOS частный — MIT Kerberos 1.22.2 собирается статически под обе архитектуры, подпись релиза проверена, sha256 в `build.env`; FreeRDP видит её через дописанный `mit-krb5.pc` и `PKG_CONFIG_ARGN=--static`, фреймворк ядра ссылается на `libresolv` и `Kerberos.framework`; NLA пробует Kerberos первым и откатывается на NTLM; у каждой сессии свой кэш билетов в памяти, уничтожается вместе с ней — FreeRDP писал бы билеты в кэш пользователя по умолчанию; тестовая область: `build-test-server.sh` собирает KDC и sample-сервер с патчем `--kerberos-keytab` (только NLA и только Kerberos), `build-core.sh` поднимает их на каждый прогон, CI собирает собеседников до ядра — [решение 23](decisions.md), [kerberos.md](knowledge/freerdp/kerberos.md); тесты: ядро — arm64 47/47, санитайзеры 46/46, x86_64 47/47, из них вход `user@REALM` и `realm\user`, неверный пароль и кэш, ушедший с сессией; клиент — 125/125 на arm64 и x86_64; **вход в настоящий домен проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 10 — _добавлено вне спецификации, 2026-09-24, решение 14_
- [x] **1.11 H.264 в RDPGFX (AVC420 и AVC444): выбор декодера — FFmpeg с аппаратным ускорением VideoToolbox против своего модуля на VideoToolbox, разбор лицензий, сборка и проверка на живом хосте; до него клиент объявляет серверу, что H.264 не принимает** — ✅ (2026-09-25, `next`) свой модуль на VideoToolbox: ускорение FFmpeg апстрим держит экспериментальным, FFmpeg — LGPL, программный декодер с патентами и разбор недоверенного потока в процессе, OpenH264 покрыт патентами только в бинарях Cisco; декодер `core/freerdp/src/libfreerdp/codec/h264_videotoolbox.c` вписан во FreeRDP патчем `h264-videotoolbox.patch` — сборка теперь идёт из копии подмодуля с дополнениями VibeRDP; Annex B в описание формата и сэмплы, синхронное декодирование, пересоздание сессии, родной NV12 в I420 с запасом; ядро объявляет AVC420 и AVC444; найден и исправлен патчем `avc444v1-sse-chroma.patch` дефект FreeRDP: SSE-сборка AVC444 v1 теряла цветность на x86_64 — [решение 24](decisions.md), [h264.md](knowledge/freerdp/h264.md); тесты: кодер VideoToolbox → API FreeRDP → пиксели: AVC420 640×480 и 1920×1080 — средняя разница 1,29 из 255, AVC444 v1 и v2 — около 2 против 57 без вспомогательного кадра, смена размера, испорченные потоки; ядро — arm64 53/53, санитайзеры 52/52, x86_64 53/53; клиент — 125/125 на arm64 и x86_64; **поток с настоящего Windows и восстановление после сна Мака проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 11 — _добавлено 2026-09-25, решение 15_
- [ ] 1.12 Прокси KDC через RD Gateway (`kdcproxyname` и `rdgiskdcproxy` в `.rdp`): FreeRDP шлёт через прокси только первичный запрос билета — нужен патч для билетов службы, Kerberos с TLS и проверка сертификата прокси системой — см. [kerberos.md](knowledge/freerdp/kerberos.md) — _добавлено вне спецификации, 2026-09-25, по итогам 1.10; в v0.1 не входит без решения владельца_

- [x] **1.13 Импорт подключений из Windows App** — ✅ (2026-09-25, `next`) «Файл → Импортировать из Windows App…» и кнопка в пустом списке; база Core Data Windows App читается с копии вместе с журналом WAL, схема проверяется до запроса, `.rdp` закладки идёт через тот же `RdpFile`, имя, пользователь и шлюз — из таблиц; пароли остаются в связке Windows App, рабочие области AVD не переносятся — [решение 30](decisions.md), [windowsAppStore.md](knowledge/macos/windowsAppStore.md); тесты на базе-образце схемы 11.4.1 с записями в журнале, запрос проверен на копии настоящей базы; клиент — 147/147 на arm64 и x86_64; **вопрос macOS о данных другого приложения проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 6 — _добавлено по просьбе владельца, 2026-09-25_
- [x] **Русский по умолчанию** — ✅ (2026-09-25, `next`) интерфейс по умолчанию на русском при любом языке системы, выбор языка запоминается, пропавший язык откатывается на русский; вариант «Как в системе» убран; вопросы macOS по-прежнему на языке системы — [решение 31](decisions.md), по правилу владельца 25.09.2026
- [x] **Готовое приложение в `dist/` проекта** — ✅ (2026-09-25, `next`) `build-client.sh` после проверок и тестов кладёт `VibeRDP.app` в `dist/`, папка исключена из git; дерево сборки Xcode остаётся в кэше, CI пакует артефакт из `dist/` — [devSetup.md](manuals/devSetup.md), по указанию владельца

## Этап 2 — Буфер обмена (MVP, релиз v0.1)
- [x] **2.1 Текст в обе стороны с опросом `changeCount` и защитой от эха — см. [pasteboardPrivacy.md](knowledge/macos/pasteboardPrivacy.md)** — ✅ (2026-09-25, `next`) протокол CLIPRDR и преобразование форматов — в ядре (`core/src/clipboard.c`, `cliptext.c`), NSPasteboard — в приложении (`client-macos/Clipboard/ClipboardBridge.swift`); с Мака в Windows: опрос `changeCount` каждые 250 мс, предложение хранится в ядре и уходит при каждом Monitor Ready, данные — по запросу Windows; из Windows на Мак: элемент с провайдером, данные с сервера при вставке, синхронно с пределом 10 с; эхо отсекается своим счётчиком, поздний ответ сервера выбрасывается; `CF_UNICODETEXT`, CRLF и LF, битые последовательности — U+FFFD — [решение 25](decisions.md), [clipboard.md](knowledge/rdp/clipboard.md), [pasteboard.md](knowledge/macos/pasteboard.md); тесты: ядро — преобразования, протокол на поддельном канале, круговой обмен с сервером-эхо (патч `sample-clipboard.patch`), arm64 67/67, санитайзеры 66/66, x86_64 67/67; клиент — мост на частном буфере и живой круговой обмен, 133/133 на arm64 и x86_64; **работу с Windows и предупреждения macOS о чтении буфера проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 12
- [x] **2.2 HTML и RTF** — ✅ (2026-09-25, `next`) «HTML Format» и «Rich Text Format» — зарегистрированные форматы Windows, ядро узнаёт их по имени и объявляет под своими номерами; HTML Мака получает заголовок «HTML Format» с метками вокруг содержимого `<body>`, HTML Windows приходит страницей от `StartHTML` до `EndHTML`, без неё — фрагментом (`core/src/cliphtml.c`); RTF — как есть, к Windows с нулём; мост объявляет HTML для Мака как UTF-8 — без этого программы Мака читают его как Latin-1 — и переводит HTML Мака в UTF-16 в UTF-8 — [решение 26](decisions.md), [clipboard.md](knowledge/rdp/clipboard.md), [pasteboard.md](knowledge/macos/pasteboard.md); сервер-эхо возвращает текст, HTML и RTF; тесты: ядро — arm64 76/76, санитайзеры 75/75, x86_64 76/76; клиент — 136/136 на arm64 и x86_64; **оформление из Word, WordPad, Edge и Safari проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 13
- [x] **2.3 Изображения** — ✅ (2026-09-25, `next`) приложение говорит с ядром PNG, ядро объявляет Windows «PNG» и `CF_DIB` — 24 бита поверх белого, а из предложенного сервером берёт «PNG», затем `CF_DIBV5`, затем `CF_DIB`; растр проверяется и перекладывается в PNG через ImageIO (`core/src/clipimage.c`); форматы Windows в ядре — таблица с порядком предпочтения вместо ветвлений; мост берёт с Мака PNG или TIFF, картинку с Windows ставит как PNG и TIFF и тянет с сервера один раз, копия картинки ждёт до 60 секунд — [решение 27](decisions.md), [clipboard.md](knowledge/rdp/clipboard.md), [pasteboard.md](knowledge/macos/pasteboard.md); сервер-эхо возвращает и `CF_DIB`; тесты: ядро — arm64 86/86, санитайзеры 85/85, x86_64 86/86, из них растры с палитрой, масками, сверху вниз и `CF_DIBV5` с альфой; клиент — 138/138 на arm64 и x86_64; **вставку в Paint, Word, Edge и Просмотр проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 14
- [x] **2.4 Файлы (FileGroupDescriptorW ⇄ file promises), прогресс для больших файлов** — ✅ (2026-09-25, `next`) обещания файлов Finder из буфера не вставляет — вместо них ссылки на файлы, которые мост тянет во временную папку при первой вставке, над окном с прогрессом и отменой; Мак → Windows: ядро обходит папки, строит FileGroupDescriptorW с именами в составной форме и само отвечает на вопросы о размере и диапазонах; Windows → Мак: имена сервера проверяются, копия идёт по мегабайту, файлы только новые, время записи из списка (`core/src/clipfiles.c`); новые API `VRCClipboardFormatFiles`, `VRCSessionCopyRemoteFiles`, `VRCResultCancelled`; при файлах на Маке картинка не предлагается — [решение 28](decisions.md), [clipboard.md](knowledge/rdp/clipboard.md), [pasteboard.md](knowledge/macos/pasteboard.md); сервер-эхо забирает файлы списком и содержимым и отдаёт их обратно; тесты: ядро — arm64 94/94, санитайзеры 93/93, x86_64 94/94; клиент — 142/142 на arm64 и x86_64, из них круг файлов через сервер-эхо с окном прогресса; **Finder и Проводник, файл больше 1 ГБ и отмену проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 15
- [x] **2.5 Чек-лист ручной проверки для оператора, сравнение поведения с Windows App** — ✅ (2026-09-25, `next`) [acceptanceChecklist.md](manuals/acceptanceChecklist.md): подготовка и файлы для проверок, первый запуск сборки без подписи, журнал движка через `WLOG_LEVEL`, порядок прохода разделов liveChecks, двенадцать сценариев рядом с Windows App — от копирования на Маке при открытом окне удалённого стола, главной боли, до видео и нагрузки, — список того, чего в v0.1 нет, форма итога и описания дефекта; сравнение — отдельным документом, а не разделом liveChecks — [решение 29](decisions.md); **проход выполняет оператор**
- [ ] 2.6 Сборка `.dmg`, Sparkle, релиз v0.1
  - [x] **Иконка и образ `.dmg`** — ✅ (2026-09-25, `next`) иконка выбрана владельцем из вариантов: экран со связью из «Связи», два окна наружу, фиолетовый из «Окон наружу» — `client-macos/Resources/icon/appIcon.svg`, размеры рисует `render-icon.sh`; `build-dmg.sh` — фон с версией на каждую сборку, стрелка из окон, под подписями «экраны» с подставкой, раскладка через `ds_store` без Finder, проверка смонтированного образа; подписи сидят по центру — замер `checkCentering.py`: сдвиг 1,5 и 0,5 точки; ссылка на «Программы» названа по-русски, как текст окна — [решение 32](decisions.md), [dmgInstaller.md](knowledge/macos/dmgInstaller.md), [devSetup.md](manuals/devSetup.md)
  - [ ] Sparkle: ключи, лента обновлений — ждёт решений владельца
  - [ ] Журнал диагностики выключить по умолчанию перед релизом: `DiagnosticsSettings.enabledByDefault = false` — [решение 34](decisions.md)
  - [ ] Релиз v0.1: публикация — решение владельца 2026-09-26: релиз в конце, когда продукт завершён, а не после этапа 2; до тех пор сборки — только для живых проверок — [решение 40](decisions.md)
- [ ] **Доводка по живой проверке владельца на Windows в домене** — _добавлено 2026-09-26_
  - [x] **Вывод кадра через displayLink** — ✅ (2026-09-26, `next`) окно сессии на настоящем Windows оставалось пустым: `needsDisplay` у вида на `CAMetalLayer` не доходит до `updateLayer`; теперь кадр рисует `NSView.displayLink`, только после изменения — [решение 33](decisions.md), [metalRendering.md](knowledge/macos/metalRendering.md); тест `testChangedFramesReachTheScreen`
  - [x] **Журнал диагностики** — ✅ (2026-09-26, `next`) движок и приложение пишут в `~/VibeRDP/logs/`, файл на запуск, десять последних; вкладка «Диагностика» в настройках; ядро пишет обмен буфером без содержимого; тестовые прогоны журнал не открывают — [решение 34](decisions.md), [diagnosticsLog.md](knowledge/freerdp/diagnosticsLog.md), [xcodeClient.md](knowledge/macos/xcodeClient.md), мануал [diagnostics.md](manuals/diagnostics.md); тесты `logTests`, `DiagnosticsTests`; **проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 17
  - [x] **Обрыв сессии на буфере больше 1600 байт** — ✅ (2026-09-26, `next`) копия 130 строк из Windows рвала соединение: дефект FreeRDP 3.32.0 в сборке сообщения канала; патч `channel-total-length.patch` — исправление upstream `190167212` целиком — [решение 36](decisions.md), [channelChunks.md](knowledge/freerdp/channelChunks.md); тест `clipboardLargeRoundTrip` — 122 КБ текста и картинка через сервер-эхо, без патча падает; ядро — arm64 101/101, санитайзеры 100/100, x86_64 101/101; **проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 12, шаг 7
  - [x] **Сессия в своём окне** — ✅ (2026-09-26, `next`) рабочий стол открывается отдельным окном, как у Windows App, окно подключений не меняет размер; окно сессии помнит место и размер, красная кнопка отключает сразу, «Отключиться» — в заголовке — [решение 37](decisions.md); тесты `SessionWindowTests`; клиент — 161/161 на arm64 и x86_64; **проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 2
  - [x] **Версия в заголовке окна и номер патча на каждую сборку для проверки** — ✅ (2026-09-26, `next`) заголовок окна подключений — «VibeRDP 0.1.1»; каждая сборка, которая уходит владельцу на проверку, поднимает патч `VERSION` в `build.env` — просьба владельца; тест `testMainWindowIsShown`
  - [x] **«Строка 1» вместо «строки 2»** — ✅ (2026-09-26, `next`, 0.1.2) две копии подряд на Маке — в Windows вставлялась первая: живая Windows отклоняет следующий список Мака ответом `CB_RESPONSE_FAIL`, пока в её буфере прежний; ядро повторяет отклонённый список через 0,5, 2 и 5 с, Windows принимает его с первого повтора; след канала буфера FreeRDP — в журнале целиком — [решение 38](decisions.md), [clipboard.md](knowledge/rdp/clipboard.md); тест `turnedDownListGoesAgain`; **подтвердил владелец** на Windows в домене — [liveChecks.md](manuals/liveChecks.md), раздел 12, шаг 8
  - [ ] Переподключение после ошибки канала: живая Windows отвечает `ERRINFO_RPC_INITIATED_DISCONNECT`, сессия не восстанавливается — разобрать — [channelChunks.md](knowledge/freerdp/channelChunks.md)
  - [x] **«Cannot find KDC for realm RTMIS.RU»** — ✅ (2026-09-26) разобрано: DNS Мака владельца не знает домена, записей SRV KDC не найти, откат на NTLM правильный — [kerberos.md](knowledge/freerdp/kerberos.md)
  - [ ] Снять патч `channel-total-length.patch` при переходе на релиз FreeRDP с коммитом `190167212`

## Этап 3 — Удобство Desktop-режима
- [x] **3.1 Динамическое разрешение при ресайзе окна (Display Control)** — ✅ (2026-09-26, `next`) ядро: канал Display Control ждёт возможностей сервера и шлёт только новый размер, ширина чётная, стороны 200–8192; `VRCSessionResizeDesktop` идёт очередью ввода; клиент шлёт размер окна после 0,3 с без изменений — [решение 35](decisions.md), [displayControl.md](knowledge/freerdp/displayControl.md); тесты `displayTests`, `testResizeIsReported`, `testDesktopFollowsTheWindowOnceItStops`; живой Windows принял 3008×1942 и 2350×1286 — по журналу владельца; **остальное проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 16
- [ ] 3.2 Мультимонитор и Retina scale factor
  - [x] **Retina: пиксель в пиксель с масштабом Windows** — ✅ (2026-09-26, `next`, 0.1.6) стол заказывается в пикселях экрана с масштабом 200% и масштабом устройства 180; перенос окна между экранами разной плотности заказывает стол заново; настройка «Чёткость на Retina» у подключения, включена по умолчанию; фиксированный размер — 100% — [решение 41](decisions.md), [displayControl.md](knowledge/freerdp/displayControl.md); тесты `scales`, `newScaleGoes`, `testSharpDesktopTakesThePixelsOfItsDisplay` на обоих мониторах стенда, `testDesktopRequestOfPoints`; **проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 16
  - [ ] Мультимонитор: сессия на все мониторы Мака (`UseMultimon`, раскладка нескольких мониторов в Display Control); учесть перепутанные масштабы в `freerdp_settings_enforce_monitor_exists` — [решение 41](decisions.md)
- [x] **3.6 Режим экрана у подключения: по окну, развёрнутое окно, во весь экран, фиксированный размер** — ✅ (2026-09-26, `next`, 0.1.3–0.1.4) раздел «Экран» в подключении; «Развёрнутое окно» — на всё свободное место экрана в одно касание (0.1.4); в 0.1.4 исправлено запоминание размера окна «По окну» — контроллер окна не держал имя сохранения; «Во весь экран» открывает сессию полноэкранной и заказывает стол размером экрана; фиксированный размер — из списка или свой, 200–8192, ширина чётная, окно по размеру стола, запросов размера нет; импорт `.rdp` и Windows App берёт режим из `screen mode id`, `dynamic resolution`, `desktopwidth`/`desktopheight` — [решение 39](decisions.md), [rdpFiles.md](knowledge/rdp/rdpFiles.md); тесты `testDisplaySettings`, `testDisplaySettingsAreKept`, `testDesktopSizeStaysWithinTheProtocol`, `testFixedDesktopKeepsItsSize`, `testFullScreenAsksForTheScreen`, `testDesktopAndWindowSizes`; **проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 18 — _добавлено владельцем 2026-09-26_
- [x] **3.7 Главное окно в духе Windows App** — ✅ (2026-09-26, `next`, 0.1.7) разделы «Избранное» и «Подключения», плитки или список, снимок последней сессии на плитке, своя картинка до первой, меню плитки, «+», сортировка, поиск, правка в листе, удаление с подтверждением — [решение 42](decisions.md); тесты `testVisibleProfiles`, `testFavoriteAndAddAndEdit`, `testDeleteTakesThePicture`, `testSnapshotOfTheDesktop`; **проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 19 — _добавлено владельцем 2026-09-26_
- [x] **Место и размер окон при смене экранов** — ✅ (2026-09-26, `next`, 0.1.8) главное окно и окно сессии «По окну» открываются где оставлены; пропавший экран — середина экрана со строкой меню — [решение 43](decisions.md); в 0.1.11 рамку хранит своё `WindowFrameKeeper`, а не AppKit, который переносил окно с MateView на главный экран; тесты `WindowPlacementTests` — _добавлено владельцем 2026-09-26_
- [x] **Перестановка плиток перетаскиванием, как в Dock** — ✅ (2026-09-26, `next`, 0.1.9) плитка приподнимается и едет за курсором, остальные расступаются пружиной; строки списка тоже; сортировка «Вручную», вид и сортировка запоминаются — [решение 44](decisions.md); тесты `testDragReorders`, `testListMoveTarget`; рывки при обмене исправлены в 0.1.10; **эффект подтвердил владелец** 2026-09-26 — «идеально» — _добавлено владельцем 2026-09-26_
- [ ] 3.3 Синхронизация раскладки RU/EN: замер вариантов А и Б, выбор, реализация — см. [keyboardLayout.md](knowledge/windows/keyboardLayout.md)
- [x] **3.4 Звук (rdpsnd)** — ✅ (2026-09-26, `next`, 0.1.12) каналы rdpsnd и rdpdr в сборке, вывод через подсистему `mac` FreeRDP (AudioQueue, PCM); у подключения «Звук Windows» — на Маке, на удалённом, не воспроизводить; импорт `audiomode`; журнал пишет подключённые каналы — [решение 45](decisions.md), [clientLifecycle.md](knowledge/freerdp/clientLifecycle.md); тесты: живое подключение со звуком `testSoundChannelsLoad`, неверный режим в ядре, `testAudioMode`; **звук на настоящем Windows проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 20
  - [x] **Микрофон** — ✅ (2026-09-26, `next`, 0.1.16) канал `audin`, подсистема `mac`, переключатель у подключения, по умолчанию выключен, `audiocapturemode` из `.rdp` — [решение 47](decisions.md); **звонок или запись на Windows проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 20, шаг 6
  - [x] **Функции RDP8 из решения 9** — ✅ (2026-09-26, `next`, 0.1.17) автоопределение сети и heartbeat включены, UDP-транспорт выключен — решение 9, итог 2026-09-26
- [x] **3.5 Проброс папки Мака (drive redirection), опционально в профиле** — ✅ (2026-09-26, `next`, 0.1.15) канал `drive` поверх rdpdr; у подключения «Папка Мака» — выбор папки, в Windows диск с её именем, по умолчанию выключено — [решение 46](decisions.md); тест `testSharedFolderLoads`; **файлы в Проводнике проверяет оператор** — [liveChecks.md](manuals/liveChecks.md), раздел 21

## Этап 4 — Протокол Seam и хелпер
- [ ] 4.1 `protocol/seam-protocol.md`: фрейминг, сообщения, версии, capability-флаги
- [ ] 4.2 Хелпер: открытие DVC, `HELLO`, ping/pong, переподключение, single-instance
- [ ] 4.3 Хелпер: перечисление и трекинг окон, фильтрация, extended frame bounds — см. [windowTracking.md](knowledge/windows/windowTracking.md)
- [ ] 4.4 Хелпер: иконки, заголовки, z-order, foreground
- [ ] 4.5 Хелпер: исполнение команд (activate/move/resize/min/max/restore/close)
- [ ] 4.6 Хелпер: установка в `shell:startup` без админа, флаг `--uninstall`
- [ ] 4.7 Клиент: DVC-аддин FreeRDP «VibeSeam», handshake с таймаутом и автоматическим fallback в Desktop
- [ ] 4.8 Unit-тесты протокола на обеих сторонах с мок-каналом

## Этап 5 — Seamless-окна
- [ ] 5.1 Менеджер окон клиента: модель удалённых окон из потока событий
- [ ] 5.2 Отдельный `NSWindow` на каждое удалённое окно, вывод субрегиона текстуры
- [ ] 5.3 Ввод в окна с трансляцией координат
- [ ] 5.4 Двусторонняя синхронизация: перемещение/ресайз на Маке ⇄ `SetWindowPos` на хосте
- [ ] 5.5 Фокус и z-order в обе стороны, смягчение артефактов перекрытий
- [ ] 5.6 Попапы, меню и тултипы (owned windows) как дочерние окна владельца
- [ ] 5.7 Переключатель режима на лету: Desktop ⇄ Seam без переподключения

## Этап 6 — Интеграция с macOS
- [ ] 6.1 Иконки удалённых приложений в Dock и в Cmd+Tab (группировка по exe)
- [ ] 6.2 Mission Control, полноэкранный режим отдельного окна
- [ ] 6.3 Лаунчер: список ярлыков «Пуск» от хелпера и запуск приложения с Мака
- [ ] 6.4 Минимизация и восстановление в обе стороны

## Этап 7 — RAIL
- [ ] 7.1 Попытка RAIL при подключении и fallback-цепочка RAIL → Seam → Desktop — см. [railSession.md](knowledge/rdp/railSession.md)
- [ ] 7.2 Вывод RAIL-окон через тот же менеджер окон, что в этапе 5
- [ ] 7.3 Документация по включению RemoteApp для тех, у кого есть админ

## Этап 8 — Релиз v1.0
- [ ] 8.1 Настройки: режимы, маппинг клавиш, интервал опроса буфера, логи — и скорость прокрутки трекпада, сейчас константа `WheelAccumulator.unitsPerPoint` — _добавлено 2026-09-25, решение 16_
- [ ] 8.2 Диагностика: экспорт логов, страница «почему не включился Seam» — см. [appLocker.md](knowledge/windows/appLocker.md)
- [ ] 8.3 README с гифками, `docs/` для хелпера
- [ ] 8.4 Подпись и нотаризация (при наличии Apple Developer ID; иначе инструкция по снятию карантина)
- [ ] 8.5 Релиз v1.0

## Предложения (без команды оператора не трогать)
- Linux-клиент на той же схеме (FreeRDP SDL + свой Seam-аддин)
- Windows-клиент с seamless-режимом
- Мульти-сессии: окна с двух хостов одновременно
- Прозрачный проброс уведомлений Windows в Notification Center
- Синхронизация тёмной темы Мака с хостом
- Drag-and-drop файлов между Mac-окнами и seamless-окнами
- Интеграционные тесты ядра против локального RDP-сервера FreeRDP (sample или shadow server): состояние Connected, каналы и NLA без Windows-машины
- Захват системных сочетаний (⌘Tab, ⌘Space и других) для Windows через `CGEventTap` — отдельной галочкой с запросом разрешения на универсальный доступ — _добавлено 2026-09-25, задача 1.4_
- Вход по билетам Kerberos, которые уже есть в macOS (расширение Kerberos SSO, `kinit`), без пароля в VibeRDP: Windows тогда не получит пароль и покажет свой вход, если не взять Remote Credential Guard (канал rdpear) — _добавлено 2026-09-25, задача 1.10_
- Пункт меню «Отправить Ctrl+Alt+Del»: отправляет Ctrl+Alt+End, чтобы не набирать ⌘⌥ + fn + → — _добавлено 2026-09-25, задача 1.4_
