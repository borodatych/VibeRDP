---
name: design-vocabulary
description: Общий словарь для работы над внешним видом интерфейса — вместо «сделай красивее» короткая команда с понятным контрактом (polish, critique, distill, bolder, quieter, typeset, layout, clarify, harden, audit, variants). Использовать, когда пользователь просит улучшить вид, отревьюить UI, «навести красоту», упростить или усилить экран, поправить типографику, отступы или тексты интерфейса.
metadata:
  vibeVersion: "1.3.0"
---

# design-vocabulary

Словарь команд для работы над интерфейсом. Каждая команда — не синоним «сделай лучше», а **контракт**: что можно трогать, что нельзя, и чем закончить.

## Зачем словарь

«Сделай красивее» — не задание: агент угадывает намерение и каждый раз угадывает по-разному. Команда из словаря задаёт направление изменения и его границы, поэтому результат воспроизводим, а спор идёт о конкретной правке, а не о вкусе вообще.

Второй источник правды — **детектор**: детерминированные проверки живой страницы (контраст, кегль, межстрочный, длина строки, зоны нажатия, геометрия, движение, признаки «сгенерированного» вида) без модели и без ключа, сразу в двух вьюпортах — десктоп и телефон 390px. Числа берутся из того, что страница реально вычислила, поэтому находку можно оспорить измерением, а не мнением. Как до него дотянуться, зависит от IDE: есть среди инструментов `design_review` — звать его самому; нет — попросить человека снять замер в окне дизайна своей IDE и прислать сводку в чат.

Третий — **контекст проекта**: `.vibe/design/product.md` (для кого продукт и чем отличается) и `.vibe/design/design.md` (палитра, гарнитуры, шкала, **именованные правила**). Есть инструмент `design_context` — он отдаёт их одним вызовом; нет — прочитать файлы напрямую. Без контекста любой результат будет обобщённым: это главная разница между «сделал ИИ» и «принадлежит этому продукту».

Четвёртый — **памятки по компонентам**: `.vibe/design/components.md` (через `design_context` или напрямую) — что не забыть у формы, поля, модалки, таблицы, пустого состояния и загрузки. Это единственный слой, который работает ДО создания: детектор меряет готовую страницу и состояния отправки, пустоты и возврата фокуса на ней не видит вовсе. Собираетесь строить компонент такого вида — прочитать памятку первой.

## Порядок работы

1. **Прочитать контекст** — до любых правок и до генерации: инструментом `design_context`, если он есть, иначе файлами `.vibe/design/` напрямую. Нет файлов — не выдумывать систему: предложить собрать её (где есть `design_document` — `target=product` после двух вопросов пользователю, `target=system` — снять визуальную систему с живой страницы; где нет — задать те же два вопроса и записать `.vibe/design/product.md` самому). Есть именованные правила — ссылаться на них **по имени**, когда защищаешь решение.
2. **Измерить до правок.** Прогнать детектор (`design_review` или замер человека в окне дизайна) и запомнить сводку. Где детектор встроен в превью, находки отмечаются прямо на странице (клик по отметке отправляет её в чат, кнопка ⚑ в тулбаре превью убирает разметку). Без замера «стало лучше» недоказуемо.
3. **Выбрать команду** из таблицы ниже по тому, ЧТО не так, а не по силе желания что-то поменять.
4. **Соблюсти контракт команды** — в первую очередь запреты.
5. **Измерить после** и показать разницу: что ушло, что появилось, что осталось осознанно.

Если детектор молчит («не измеряли»), причину называет его диагностика (где есть — `design_doctor`): превью закрыто, в странице нет моста IDE (статическое превью несёт его всегда, dev-сервер — через прокси, Docker — не несёт), контекста нет или id принятого отклонения написан с опечаткой.

Правки вида — всегда отдельный проход. Не смешивать с изменением поведения: перепутанные в одном диффе «поменял отступы» и «поменял логику» невозможно отревьюить.

## Команды

| Команда | Когда | Контракт |
|---|---|---|
| **critique** | «Посмотри и скажи, что не так» | Только разбор, **ни одной правки**. Список проблем с приоритетом, каждая — с элементом и причиной. Заканчивать вопросом «что чиним». |
| **audit** | Перед показом людям | Технические проверки: контраст, размеры целей, порядок заголовков, поведение на узком экране, состояния фокуса. Чинить только найденное детектором, не «заодно». |
| **polish** | Почти готово, надо довести | Мелкая доводка: выравнивания, отступы по сетке, единообразие радиусов и теней. **Запрещено** менять композицию, палитру и тексты. |
| **distill** | Перегружено | Убрать лишнее: декор без функции, дубли, украшения ради украшений. Ничего не добавлять — только вычитать. |
| **bolder** | Пресно, безлико | Усилить контраст, ритм и акцент. Один акцент на экран, не три. **Запрещено** трогать доступность ради выразительности. |
| **quieter** | Кричит, рябит | Приглушить: убрать свечения, снизить насыщенность, успокоить анимации. Иерархия должна сохраниться — тише не значит однороднее. |
| **typeset** | Текст «не читается» | Только типографика: кегли, интерлиньяж, длина строки, начертания, иерархия заголовков. Разметку и цвета не трогать. |
| **layout** | Криво стоит | Только сетка, отступы и порядок блоков. Ритм отступов — из шкалы, а не «на глаз». Контент не переписывать. |
| **clarify** | Непонятные надписи | Только тексты интерфейса: подписи кнопок, подсказки, ошибки, пустые состояния. Говорить, что произойдёт, а не как всё замечательно. Тексты длиннее подписи (герой, описания, лендинг) — по навыку `anti-slop`. |
| **harden** | Готовим к продакшену | Крайние случаи: длинный текст, отсутствие данных, ошибка загрузки, медленная сеть, узкий экран. Добавлять состояния, а не украшения. |
| **animate** | Статично и мертво, но трогать композицию нельзя | Только моторика: появление карточек, построчный вывод в терминале, рамка на наведении **только у кликабельного**. Длительности из шкалы, не «на глаз»; ускорение без перелёта (детектор `elastic-easing` ловит пружину). **Запрещено** анимировать свойства раскладки (`height`, `top`, `width` — правило `layout-property-animation`) и добавлять движение, которое нельзя отключить: `prefers-reduced-motion` уважается всегда. Одна идея движения на экран — три разных значат, что моторики нет, есть суета. |
| **variants** | «Не уверен, как лучше» | **Три** варианта одной правки, не больше: каждый — отдельным диффом, с одной строкой «в чём идея» и честным «чем платим». Варианты обязаны расходиться **по осям различия** (см. ниже), а не оттенком одного решения. Каждый из трёх должен быть пригоден: два заведомо слабых «для контраста» — это не выбор, а его имитация. Ничего не применять до ответа: пользователь называет номер, и только выбранный вариант уходит в файл. |

### Оси различия для `variants`

Варианты считаются разными, если расходятся хотя бы по одной оси **целиком**, а не на величину:

| Ось | Что меняется |
|---|---|
| Иерархия | что здесь главное и что читается вторым |
| Топология раскладки | как блоки стоят друг к другу: колонки, поток, вложенность |
| Типографика | роль гарнитур и шкала, а не кегль на два пункта |
| Цветовая стратегия | чем несётся акцент: цветом, весом, пространством |
| Плотность | сколько воздуха и сколько всего на экране |
| Структурная декомпозиция | из каких частей вообще собран блок |

Три варианта, отличающиеся отступом, кеглем и радиусом, — это один вариант, показанный трижды.

## Правила, которые сильнее любой команды

- **Числа детектора не спорят с мнением, а дополняют его.** Контраст 3.9:1 — это факт; «выглядит нормально» его не отменяет.
- **Пол качества не обсуждается, вкус — обсуждается.** Находки класса «пол» (контраст ниже нормы, зона нажатия меньше 44px, обрезанный контент, битая картинка, пропуск уровня заголовка) — дефекты при любом стиле. Стилевые находки — повод для разговора, а не приговор.
- **Осознанное отклонение пишется в файл, а не в чат.** Считаете находку идентичностью продукта — впишите `id-правила — причина` в раздел «Детектор» файла `.vibe/design/design.md`. Тогда детектор будет показывать её как решение, а не как дефект, и через месяц будет понятно почему. Устная договорённость исчезает вместе с тредом.
- **Проверить дешевле, чем починить не-баг.** Прежде чем «исправлять» найденное обрезание на мобильном, убедитесь, что оно есть: узкое окно инструмента иногда само режет раскладку.
- **Гарнитура не выбирается по ассоциации.** «Книга — значит serif», «магазин — значит рукописный», «технологии — значит моноширинный» — это первая мысль, а не решение: она приходит одинаковой всем, поэтому и результат у всех одинаковый. Выбирать по задаче текста (длина строки, кегли, язык, начертания, вес в интерфейсе), а не по теме продукта.
- **Первый подходящий вариант — не повод останавливаться.** Если гарнитура, палитра или раскладка нашлись с первой попытки и совпали с тем, что стоит в каждом втором шаблоне, — это признак инерции, а не удачи. Детектор назовёт затёртую гарнитуру находкой `overused-font`; ответ на неё — либо другой выбор, либо запись в `design.md`, почему именно эта принадлежит продукту.
- **Не заводить новые сущности ради вида.** Ещё один оттенок, ещё один радиус, ещё одна тень — сначала проверить, есть ли уже подходящий в проекте.
- **Русский в интерфейсе.** Все пользовательские строки — по-русски (правило проекта), включая подписи, подсказки и ошибки.
- **Доступность не обменивается на красоту.** Контраст ниже нормы, зона нажатия меньше 44px и пропуск уровня заголовка — дефекты, а не стиль.

## Признаки «сгенерированного» вида

Их ищет детектор, но полезно знать в лицо — это самые частые следы того, что интерфейс собрали по инерции, а не спроектировали:

градиентный текст в заголовке · фиолетово-индиговая палитра по умолчанию · кремово-бежевый фон «на всякий случай» · радиальное пятно-ореол за героем · цветное свечение вместо тени · чип-надпись над заголовком · плитка с иконкой над заголовком карточки · крупный курсивный serif в герое · затёртые гарнитуры (Inter, Geist, Space Grotesk, Instrument Serif, Manrope, Outfit, Sora, Satoshi, Clash Display, Poppins, Montserrat, Playfair Display и другие из стоп-листа) · стеклянные панели с размытием · декоративная сетка и полосы на фоне · толстая цветная полоса с одной стороны блока · тонкая рамка вместе с широкой тенью · крайний радиус на маленькой карточке · пульсирующая точка · анимация с перелётом · карточка в карточке в карточке · одинаковые карточки-клоны подряд · одинаковые отступы везде (нет ритма) · тройка «большое число + подпись» · мелкие номера у заголовков · одна гарнитура на всё · заголовки соседних уровней одного кегля · капслок на длинном тексте · выключка по ширине · висячий предлог в конце строки · одинокое слово на последней строке · маркетинговые обещания вместо описания · частые длинные тире · «...театр» как способ обесценить чужое · иллюстрация из десятка примитивов.

Ни один из них сам по себе не приговор. Приговор — когда их пять на одном экране: это и читается как «сделано генератором».

Тексты страницы детектор читает каталогом нейрослопа — тем же, что у детектора текста (правило `copy-slop`). Переписывать их — по навыку `anti-slop`: он не даёт заменить пустое обещание выдуманной цифрой, а это худшее, во что превращается такая правка.

## Как заканчивается разбор (contract вывода)

Разбор без формы превращается в поток замечаний, где похвала гасит критику, а неизмеренное выдаётся за проверенное. Поэтому `critique` и `audit` заканчиваются ровно так:

```
ВЕРДИКТ: отклонить | доработать | выпускать
```

- **Не больше двух хороших вещей.** Хвалить всё — значит обесценить остальное: если в разборе пять похвал, читатель перестаёт верить и находкам.
- **Находки — по важности**, каждая с элементом, числом или причиной. «Выглядит неаккуратно» находкой не считается.
- **Обязательный раздел «Чего я не смог оценить»**: что не проверялось и почему (не было данных, не открылась страница, состояние не воспроизводится). Пустым он не бывает — всегда есть то, что измерить нечем.
- **Зелёный детектор — не доказательство вкуса.** Он говорит только «пол качества не пробит»; вердикт «выпускать» ставится глазами, а не набором пройденных проверок.

## Чего детектор не увидит — смотреть самому

Измерить нельзя, а следы настоящие:

- **Афористичная каденция:** каждая секция заканчивается коротким опровержением или искусственным контрастом. Один раз — приём, пять раз — шаблон.
- **Иллюстрация-заглушка:** герой, собранный из кругов и прямоугольников, читается как клипарт (счёт примитивов детектор ловит, «похоже на заглушку» — нет).
- **Порядок и визуальная тяжесть:** самый тяжёлый элемент в центре, кнопки в порядке, который не ведёт взгляд. Смотреть на композицию, а не на числа.
- **Пасхалки и жизнь:** статичный «забор» карточек технически безупречен и мёртв. Одна сдержанная деталь, реагирующая на действие, отличает продукт от макета.
