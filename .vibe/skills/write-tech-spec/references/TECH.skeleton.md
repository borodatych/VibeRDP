<!--
TECH.md — заготовка. Копировать в docs/specs/<id>/TECH.md и заполнять.
Правила и принципы — в SKILL.md скилла write-tech-spec. <id> совпадает с соседним
PRODUCT.md. За пользовательским поведением — ссылаться на PRODUCT.md, не пересказывать.
Опциональные секции включать только когда добавляют сигнал; пустую удалять.
-->

# <Название фичи> — техническая спека

## Context

<!--
Что строим, как изменяемая область работает сейчас, ключевые файлы со ССЫЛКАМИ НА
СТРОКИ (не гадать — открыть код). Объединяет «проблему», «текущее состояние» и
«релевантный код» в одну заземлённую секцию.
-->

- `path/to/entry.ts:42` — <точка входа сценария.>
- `path/to/state.ts (120–220)` — <состояние/обработка, которые изменятся.>

Пользовательское поведение — см. `PRODUCT.md`.

## Proposed changes

<!--
План реализации: какие модули меняются; вводимые типы/API/состояние; поток данных;
границы владения; чем дизайн следует существующим паттернам репозитория. Где разумных
путей больше одного — явно назвать trade-off'ы и выбранный вариант.
-->

- <Модуль/файл: что меняется и почему так.>
- <Вводимый тип/API/состояние.>

## Testing and validation

<!--
Как реализация проверяется против поведения. Ссылаться на нумерованные инварианты
Behavior из PRODUCT.md напрямую: каждый важный инвариант → конкретный тест/шаг.
Валидация живёт ЗДЕСЬ (в PRODUCT.md секции Validation нет).
-->

- PRODUCT.md #<N> → <unit/интеграционный тест или ручной шаг.>
- PRODUCT.md #<N> → <проверка.>

<!-- Опциональные секции — только когда добавляют сигнал:
## End-to-end flow      — если трасса через систему говорит больше, чем список изменений.
## Diagram              — Mermaid, когда визуал объясняет дизайн быстрее прозы.
## Risks and mitigations — реальные режимы отказа, регрессии, миграции, риски раскатки.
## Parallelization      — когда работу можно чисто разделить между агентами.
## Follow-ups           — отложенная уборка / будущая работа.
-->
