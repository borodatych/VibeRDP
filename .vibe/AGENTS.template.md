# AGENTS.md

<!--
  ЭТО ШАБЛОН. Сам по себе он не действует: правила читаются из AGENTS.md в КОРНЕ проекта,
  а этот файл лежит в `.vibe/` как заготовка. Скопируйте его в корень командой
  «VibeIDE: Создать AGENTS.md» или руками — и правьте уже там.

  Засевается он шаблоном намеренно: AGENTS.md — файл вашего репозитория, он едет в коммит и его
  читают чужие агенты. Создавать его в корне без спроса значит писать в чужой репозиторий.

  Формат — agents.md (60 000+ репозиториев, поддержан Codex, Cursor, Copilot Coding Agent, Junie, Zed, Warp и другими).
  Его читают и VibeIDE, и VibeIDEA — наравне с `.vibe/rules.md` и деревом `.vibe/rules/**`.

  ЗАЧЕМ ОН, ЕСЛИ ЕСТЬ `.vibe/rules.md`:
  `.vibe/rules.md` — правила для нашего инструмента. AGENTS.md видят ЧУЖИЕ агенты, приходящие
  в тот же репозиторий: Codex в терминале, Copilot в пул-реквесте, агент коллеги. Одни и те же
  правила, записанные в оба файла, разъедутся; поэтому держите содержание ЗДЕСЬ, а в
  `.vibe/rules.md` — только то, что относится к самой IDE.

  МОНОРЕПОЗИТОРИЙ: по стандарту агент читает БЛИЖАЙШИЙ файл вверх по дереву, поэтому в каждом
  пакете можно положить свой AGENTS.md — он перекроет корневой для файлов этого пакета.

  Обязательных разделов нет: это обычный markdown. Ниже — заготовка, из которой удаляют
  ненужное, а не заполняют всё подряд. Пустой раздел хуже отсутствующего: он выглядит
  ответом и им не является.
-->

## Что это за проект

<!-- Одна-две строки: что собирается, для кого, чем запускается. Агент не угадывает стек. -->

## Команды

<!--
  Только то, что реально работает в чистом клоне. Команда, которой нет, дороже отсутствия
  раздела: агент потратит ход, получит ошибку и пойдёт искать обходной путь.
-->

| Что | Команда |
|---|---|
| Установка зависимостей | `` |
| Сборка | `` |
| Тесты | `` |
| Линт / типы | `` |

## Границы

<!--
  Куда писать нельзя и почему. Пути от корня проекта.
  Пример: сгенерированные файлы правятся генератором, а не руками; миграции не переписываются
  задним числом; секреты не попадают в репозиторий.
-->

## Стиль кода

<!--
  Только то, что НЕ проверяет линтер. Всё, что линтер и так ловит, повторять здесь вредно:
  два источника правды расходятся, и агент верит не тому.
-->

## Проверка перед завершением

<!--
  Чем агент обязан убедиться, что не сломал сборку, — списком команд по порядку.
  Без этого раздела «готово» означает «я так думаю».
-->

## Чего делать не нужно

<!--
  Самый полезный раздел и самый редкий. Например: не обновлять зависимости заодно с правкой;
  не переформатировать чужие файлы целиком; не заводить новый способ делать то, что уже
  делается в проекте одним способом.
-->
