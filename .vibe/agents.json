// ─────────────────────────────────────────────────────────────────────────────
//  .vibe/agents.json — внешние агенты проекта (проектный формат набора)
//
//  Это РАБОЧИЙ файл, а не пример: файла-двойника рядом больше нет. Комментарии писать
//  МОЖНО — формат JSONC, как и у остальных конфигов .vibe.
//  Все записи выключены ("active": false) — снимите поле у той, которой будете пользоваться.
//  ВНИМАНИЕ, ОБЛАСТИ: этот файл описывает агентов ПРОЕКТА и едет в репозитории. Машинный
//  реестр агентов — деталь конкретного продукта: он есть не у всех, путь у каждого свой,
//  и набор его намеренно не называет — смотрите документацию своего продукта. Там, где он
//  есть, проектная запись сильнее машинной при совпадении имени.
//
//  Зачем: описывает, кого можно позвать в эту рабочую папку кроме собственного
//  агента самой IDE. Внешний агент запускается отдельным процессом и говорит с IDE
//  на ACP (Agent Client Protocol) — спрашивает разрешение перед правкой, а IDE
//  снимает по его правкам чекпоинт и пишет их в журнал.
//  Нет файла — внешних агентов нет; всё остальное работает как раньше.
//
//  Не хотите писать руками — попросите агента в чате:
//    «Создай мне .vibe/agents.json для этого проекта»
//  (полная спека формата — docs/manuals/agentsSpec.md, её можно скормить любой LLM).
//
//  Главные принципы:
//   • "id" + "command" — единственные обязательные поля.
//   • Команда запускается НАПРЯМУЮ, без оболочки: аргументы перечисляются
//     по одному в "args", а не пишутся строкой в "command".
//   • Агент должен уметь ACP. Обычный CLI сюда не годится: у большинства
//     инструментов режим ACP даёт отдельный адаптер (см. пример ниже).
//   • Одна битая запись не роняет реестр — пропускается с предупреждением.
//   • "active": false оставляет запись документированной, но вне списка.
//   • "registry": {"id", "version"} — откуда запись: id агента в реестре ACP и версия, которую
//     она запускает. Ставит импорт из реестра, руками писать не нужно. По нему каталог узнаёт уже
//     добавленного агента под любым именем, а обновление — какую версию запись запускает сейчас.
// ─────────────────────────────────────────────────────────────────────────────
{
	"version": 1,
	"agents": [
		// ─── Claude через официальный ACP-адаптер ────────────────────────────
		// Требует входа: выполните `claude /login` в терминале один раз.
		//
		// ИМЯ «Claude Agent», А НЕ «Claude Code»: правила брендинга Agent SDK разрешают «Claude Agent» и прямо
		// запрещают «Claude Code» и «Claude Code Agent» в чужих продуктах (code.claude.com/docs/en/agent-sdk/overview,
		// сверено 15.09.2026). Запомненные под старым именем сессии продукт переносит сам.
		//
		// ЛИМИТЫ ПОДПИСКИ: из стороннего IDE через адаптер сейчас расходуются лимиты вашего плана Pro/Max
		// (support.claude.com/en/articles/15036540, сверено 15.09.2026). Anthropic собирался с 15.06.2026 перевести
		// такие запуски на отдельный кредит по ценам API и отложил это — если план вернут, расход станет кредитным.
		// Сам `claude` разговаривать по ACP не умеет — адаптер обязателен.
		//
		// ВНИМАНИЕ, ЧЕМ ВЫ ПЛАТИТЕ: подписку МОЛЧА перебивает окружение. ANTHROPIC_AUTH_TOKEN и
		// ANTHROPIC_API_KEY уводят на оплату за токены, CLAUDE_CODE_USE_BEDROCK / _VERTEX /
		// _FOUNDRY — в облако провайдера, и лимиты плана перестают действовать
		// (code.claude.com/docs/en/authentication, проверено 11.09.2026). Держите ключ Anthropic в
		// настройках IDE, а не в окружении, либо снимайте переменную для этой записи пустым
		// значением: "env": { "ANTHROPIC_API_KEY": "" } — пустое значение означает «убрать».
		//
		// Версия НЕ ПРИПИНОВАНА намеренно — «свежий адаптер» удобнее в чате. Нужна
		// воспроизводимость (CI, общая на команду настройка) — допишите её прямо в имя пакета:
		// "args": ["@agentclientprotocol/claude-agent-acp@0.79.0"]. В 0.77.0 удалена настройка
		// claudeCode.options.agent — если закрепляли старее и пользовались ею, перечитайте его выпуск. Официальный реестр
		// протокола пинует версию именно так, и кнопка «Каталог ACP-агентов» это наследует — и показывает «есть новее»,
		// когда реестр ушёл вперёд.
		//
		// ФЛАГА `-y` НЕТ НАМЕРЕННО: npm сам подразумевает `--yes`, когда stdin не терминал, а у ACP-агента stdin —
		// канал протокола, то есть не терминал никогда (docs.npmjs.com/cli/v11/commands/npm-exec, сверено 23.09.2026).
		// Флаг ничего не менял, но читался проверками команд как «ставит без спроса».
		{
			"id": "claude", "active": false,
			"name": "Claude Agent",
			"command": "npx",
			"args": ["@agentclientprotocol/claude-agent-acp"]
		},

		// ─── OpenHands (нативный режим ACP) ─────────────────────────────────
		// Адаптер не нужен: CLI умеет ACP сам, подкоманда — `acp`, транспорт JSON-RPC по stdio
		// (docs.openhands.dev/openhands/usage/cli/ide/overview, проверено 09.09.2026).
		// Лицензия MIT.
		//
		// Установка одним из двух способов, интерпретатор нужен свой:
		//   uv tool install openhands --python 3.12     (нужен Python 3.12+)
		//   curl -fsSL https://install.openhands.dev/install.sh | sh
		// На Windows работает ТОЛЬКО через WSL.
		//
		// В официальном реестре ACP его нет — поэтому запись здесь, а не в каталоге агентов.
		//
		// ПЕСОЧНИЦЫ В ЭТОМ РЕЖИМЕ НЕТ: CLI исполняет команды агента прямо на хосте, без изоляции
		// (github.com/OpenHands/OpenHands-CLI/issues/472, сверено 11.09.2026). Изолированный запуск
		// у OpenHands живёт в другом месте — Agent Canvas и SDK (Docker, VM), а не `openhands acp`.
		// CLI БОЛЬШЕ НЕ РАЗВИВАЕТСЯ: README github.com/OpenHands/OpenHands-CLI прямо пишет «no longer
		// actively maintained» и отправляет в Agent Canvas (сверено 13.09.2026). Запись оставлена, потому
		// что команда по-прежнему работает, но исправлений ждать не от кого. Agent Canvas и SDK — это
		// ACP-КЛИЕНТЫ для чужих агентов, а не агент для IDE: заменой этой записи они не являются.
		// Подтверждения у него свои, три режима: Always Ask (по умолчанию), Always Approve и
		// LLM-Based; переключаются на лету командой `/confirm always-ask`. Наши diff-превью,
		// чекпоинты и журнал при этом работают как обычно — они на стороне IDE.
		{
			"id": "openhands",
			"name": "OpenHands",
			"command": "openhands",
			"args": ["acp"],
			"active": false
		},

		// ─── Cursor Agent (нативный режим ACP) ──────────────────────────────
		// Адаптер не нужен: CLI Cursor умеет ACP сам. Бинарь называется `agent`
		// (он ставится вместе с приложением Cursor, обычно ~/.local/bin/agent),
		// подкоманда — `acp`. Ключ передаётся флагом ДО подкоманды, поэтому в
		// "args" он идёт первым: ["--api-key", "…", "acp"].
		// Режимы agent / plan / ask — это штатные режимы сессии ACP: переключаются
		// пикером над композером, plan и ask доступны только на чтение.
		// MCP-серверы берутся из .cursor/mcp.json (проектный и пользовательский);
		// серверы уровня команды из дашборда Cursor в режиме ACP НЕ подхватываются.
		// Вход: `cursor login` один раз в терминале. Ключ в файл репозитория не
		// пишите — либо вход, либо CURSOR_API_KEY в окружении машины и запись вида
		// "args": ["--api-key", "$CURSOR_API_KEY", "acp"] в СВОЁМ agents.json.
		// Источник: cursor.com/docs/cli/acp (проверено 08.09.2026).
		{
			"id": "cursor",
			"name": "Cursor Agent",
			"command": "agent",
			"args": ["acp"],
			"active": false
		},

		// ─── Kimi Code CLI (нативный режим ACP) — путь к подписке Kimi Code ──
		// Адаптер не нужен: CLI Kimi Code умеет ACP сам, подкоманда — `acp`. Для IDE JetBrains вендор
		// предлагает именно этот путь: IDE запускает `kimi acp` (kimi.com/code/docs, разделы
		// kimi-code-cli/guides/ides и reference/kimi-acp, сверено 23.09.2026). В официальном реестре ACP
		// агент есть под id `kimi` (бинарь, лицензия MIT).
		//
		// Клиентом остаётся программа вендора, поэтому подписка расходуется так, как задумал он сам. Условия
		// подписки: только интерактивная работа — скриптовая и пакетная автоматизация запрещена
		// (раздел community-guidelines там же).
		//
		// Установка — по инструкции вендора. Вход: один раз запустите `kimi` в терминале и пройдите вход;
		// без него сессия не откроется («auth required»). Если IDE не находит команду, впишите абсолютный
		// путь из `which kimi`.
		{
			"id": "kimi",
			"name": "Kimi Code CLI",
			"command": "kimi",
			"args": ["acp"],
			"active": false
		},

		// ─── Агент для одного пакета монорепо ───────────────────────────────
		// "dir" задаёт рабочую папку относительно корня проекта: агент увидит
		// её как свой корень и не полезет в соседние пакеты.
		{
			"id": "web",
			"name": "Claude Agent (web)",
			"command": "npx",
			"args": ["@agentclientprotocol/claude-agent-acp"],
			"dir": "packages/web",
			"active": false
		},

		// ─── Свой агент с переменными окружения ─────────────────────────────
		// "env" кладётся поверх унаследованного окружения. Ключи в файл
		// репозитория не пишите — передавайте имя переменной, а значение
		// держите в окружении машины.
		{
			"id": "custom",
			"name": "Собственный ACP-агент",
			"command": "/usr/local/bin/my-acp-agent",
			"args": ["--stdio"],
			"env": { "MY_AGENT_PROFILE": "project" },
			"active": false
		}
	]
}
